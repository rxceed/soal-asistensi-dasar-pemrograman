#include <stdio>

int f(int x, int order, int n)
{
  int result;
  //hitung hitung hitung
  //kode untuk menghitung
  //hitung hitung hitung
  if(order != 1)
  {
    //rekursi.......
    return result;
  }
  else
  {
    return result;
  }
}

int main()
{
  //Bentuk persamaan polinomial: nx + nx^2 + nx^3 + ....... + nx^order
  //Misal: x = 2, order = 3, n = 2, maka hasil y = 28
  int x, order, n;
  y = f(x, order, n);
  printf("%d", y);
  return 0;
}
